apiVersion: v1
kind: Service
metadata:
  labels:
    app: simpleapp-service
  name: simpleapp-service
spec:
  ports:
  - name: simpleapp-port
    port: 50001
    protocol: TCP
    targetPort: 50001
  selector:
    app: simpleapp
  type: LoadBalancer
status:
  loadBalancer: {}
